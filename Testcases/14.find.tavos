func void main()
{
    bool found = false;
    int array[4];
    array[0]=5;
    array[1]=1;
    array[2]=7;
    array[3]=4;
    int valueSearch;
    read(valueSearch);

    for(x=0,x<4)
    {
        if(array[x] == valueSearch)
        {
            found=true;
            print("Element was found at index:",x);
        }
        x=x+1;
    }
    if(found == false)
    {
        print("Element was not found");
    }

}